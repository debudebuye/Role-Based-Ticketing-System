# REQUIRED — must be set in every environment (dev, staging, production).
# In production, omitting this will throw an error at startup to prevent
# the app from silently pointing at localhost.
VITE_API_URL=http://localhost:5000/api/v1

VITE_SOCKET_URL=http://localhost:5000
